Explanation
Tournament sort builds a binary tree where the leaves are the array elements and the internal nodes are the tournament winners (smaller elements). Further, at each step the winner is removed and the tree is rebuilt.

Operating Time Estimation
Best case:
𝑂
(
𝑛
log
⁡
𝑛
)
O(nlogn)

Average case:
𝑂
(
𝑛
log
⁡
𝑛
)
O(nlogn)

Worst case:
𝑂
(
𝑛
log
⁡
𝑛
)
O(nlogn)